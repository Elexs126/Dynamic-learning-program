# 数学一＋408 自适应备考系统：项目交接背景

更新日期：2026-09-06

## 这份文件是什么

这不是聊天窗口的逐字复制，也不是新的执行协议。

它只保存一个完全不了解本项目的新 AI 在读完《完整执行协议》《启动合同》和《系统进度》后，仍然需要知道的背景、前提、历史决策、踩坑原因和接手方式。

旧的近五千行原始对话已经只读保存在：

`系统文件/历史归档/原始对话记录_历史备份.md`

除非要追溯某一项决策原文，不要把该归档当作启动必读，也不要再把新的聊天逐字追加到本文件。

## 一、项目最初要解决的问题

用户不是要一份静态复习计划，也不是要把所有题目一次性贴满标签。目标是建立一个能持续运行的数学一＋408自适应备考系统：

- 用目标考试真题建立考试模型；
- 用教材和题库建立课程结构与能力图；
- 用学生真实作答建立学生模型；
- 根据得分、用时、提示、迁移和遗忘情况动态决定下一步；
- 用独立测评验证能力，而不是把“看懂讲解”当成掌握；
- 在每周固定时间预算内调整数学一与408资源分配。

《完整执行协议》描述了这个完整闭环。后续所有文件的任务是把协议逐步落成可执行数据，而不是重新发明另一套系统。

## 二、项目经历过的几个阶段

### 1. 原始资料整理与全局编号

项目先整理了数学一、数学二、数学三、408真题，以及张宇、王道和408经典练习题，并在 Markdown 中加入全局唯一编号。

这一阶段解决的是“题目能不能稳定定位”，没有解决“题目是否已经正式标注、能力是否掌握、下一步该学什么”。

### 2. 50题 L3 prototype

曾从数学一真题中抽取50题，使用旧 `l3-v1` 结构做多 Agent 试标。这个试验有价值，但只能证明流程原型跑过：

- 多个 Agent 曾重复读取同一批题，成本偏高；
- 中间结果有过落盘失败和恢复；
- 旧字段与后来冻结的正式字段不一致；
- 50题中仍有多个评分单元、未知子分值和 review flags；
- prototype 的教学诊断扩展字段不自动进入永久核心 schema。

因此原始 prototype 保存在 `系统文件/标签/`，迁移候选保存在 `系统文件/迁移记录/`。迁移候选全部保持 `needs_review`，不能扩写成“50题已经正式完成 L3”，更不能推断全库已经完成。

### 3. 正式字段消歧

后续对 L0—L3 字段做了反复问答和修正，最终形成这些关键规则：

- 正式主键是 `question_id`；旧 `item_id` 只在迁移入口兼容。
- L0记录原始事实和稳定来源，不靠一句 `locked:true` 代替不可变性校验。
- L1是候选粗标，必须保留来源与不确定性。
- L2保留唯一主知识点/能力行为等字段，次知识点受数量限制；2026-09-06起取消提前全量核心L2，教学字段按近期实际需要补充，主字段审核不等于整条L2完成。
- L3只在实际调用或评分结构需要时按需完成，不要求全库一次性精标。
- 使用角色不是题目永久标签，必须与 annotation 分表保存。
- 答案、完整解析、学生作答结果不得写进 annotation。

当前人读定义以 `系统文件/标签/标签说明.md` 为准，机器约束以 `系统文件/数据规范/` 为准。

### 4. 仓库核对与真实基线

交接材料曾写全库约8125题。实际按 canonical 路径和 `【唯一编号】` 扫描后是8024：

| 题源 | 唯一记录数 | 角色 |
|---|---:|---|
| 数学一真题 | 431 | 目标考试核心 |
| 408真题 | 846 | 目标考试核心 |
| 张宇基础30讲 | 876 | 核心教学辅助 |
| 数学二真题 | 431 | 辅助迁移 |
| 数学三真题 | 431 | 辅助迁移 |
| 张宇1000题 | 1041 | 配套训练 |
| 王道408 | 2897 | 配套训练 |
| 408经典练习题 | 1071 | 配套训练 |
| 合计 | 8024 | 当前实库基线 |

差额101来自王道408曾被估为2998，实际只有2897。没有来源证据时不能为了对齐旧估计而制造101条记录。

数学一431＋408 846＝1277，只表示可进入目标考试统计的候选全集。它们不能默认全部对 Teacher 可见，因为其中未来还要隔离诊断、固定审计和密封题。

数学一431＋408 846＋张宇基础30讲876＝2153，保留为核心来源候选规模。2026-09-06起不再要求提前全量L2；张宇基础30讲876题按训练调用加工。

### 5. 执行合同补齐

用户后来明确了运行所需输入：

- 预计考试时间是2026-12-20；
- Day 0是2026-09-01；
- 数学一＋408每周净学习50小时；
- 政治＋英语另有每周14小时，内部比例由用户自己动态安排；
- 数学一目标126、保底118；
- 408目标105、保底100；
- 今年大纲尚未发布，暂用项目中的上一年度数学一、408大纲；
- 主教材就是 `讲义/` 下七门课程；
- 用户自行用外部绝对正确内容核对答案、过程和正确率，系统不用提供或验证答案；
- 用户没有看过任何真题或整卷；
- “每科24—36个锚点”中的“每科”是每门独立课程，因此数学一有三组、408有四组。

用户没有单独指定首个完整周的数学一/408比例，所以系统采用25/25中性默认。该默认在 Day 0 冻结前可直接覆盖，冻结后按周审计调整。

### 6. Day 0 时间口径纠错

Day 0是2026-09-01，按协议它是启动前置，不计入第1—5日。STARTUP日历窗口是9月2—6日，且最多25个有效学习小时。

按现有每日时数，9月2—5在9月5日完成当日首3个有效小时后会达到25小时，所以应立即退出 STARTUP，而不是为了凑满五个日历日继续消耗启动题。

9月6日只做启动收口。首个完整预算周是9月7—13日，首个正式固定审计日是9月13日。

系统维护上限5小时包含在数学一＋408的50小时内，不是额外的第51—55小时。

### 7. 作答日志边界

用户掌握答案和正确率的权威来源，Teacher不参与判答案。因此正式日志只保存运行所需元数据：

- 题目编号；
- 第一遍锁定和用户自核时间；
- 第一遍得分率；
- 第一遍用时；
- 最高提示等级；
- 是否陌生、限时、混合；
- 与上次接触的间隔；
- 主要错误类型；
- 五维观察；
- 下次复习时间。

不得保存题干、答案、完整作答过程或完整解析。五维中的 `null` 表示本次没有可靠观察，不等于失败。

正式日志是 `系统文件/运行记录/attempts_v1.jsonl`。Day 0初始为0字节、0条记录；不能伪造“初始化作答”。能力状态和认证等级应由历史记录派生。

### 8. 考纲范围骨架与七门主教材 Scope 映射（2026-09-03）

为了给后续抽取正文细知识点建立稳定、低成本的前置约束，系统完成了考纲骨架 `syllabus_scope_v0`（166个Scope）及 7 门主教材（高等数学、线性代数、概率统计、操作系统、计算机网络、计算机组成原理、数据结构）的章/节/题型与 Scope 的精细化映射（`textbook_scope_mapping_*_v0`）与正文抽取批次规划（`extraction_batches_*_v0`）。

**双模交付必须完整维护**：
- 机器规范：`系统文件/配置/` 下的 `.json` 文件，用于自动化脚本、校验工具与抽取 Agent 程序化解析；
- 人读规范：`系统文件/标签/` 下的 `.md` 文件，用于人类查阅、审查与排版展示。

**历史踩坑与纠正**：
在处理 408 课程时，曾因 Agent 仅参考前序会话产物而出现“只写 Markdown、漏生成配置 JSON”的偏差，导致 408 机器规范缺失。已于 2026-09-03 全量补齐 408 全部 8 份机器读 JSON 规范并完成 100% 考纲 Scope 覆盖自动化校验。后续调整Scope、章节映射或批次结构数据时必须维持双模对齐，不得只写单模。本次2026-09-06仅修订说明类Markdown的执行策略，没有变更上述结构表或机器配置；新增策略的自动执行能力不得宣称已同步实现。

### 9. 成本控制修订（2026-09-06）

用户在限定只读方案说明Markdown的审阅后，明确要求执行修改。本轮保留整体框架，仅修订执行要求：

- 2153题核心来源不再提前全量L2；训练字段按近期需要补充，未加工内容保持候选/待审核。
- 已有208个教材批次是按需导航，细知识词典随用随建，结果复用；不默认启动全量语义归并或多模型互审。
- 每周API建设预算包含抽取、对齐、复核、重试和shadow。数值上限尚未确认，额外后台建设默认暂停，不影响当前学习必需的有限按需处理。
- Evaluator允许人工离线保管、自核并回传必要汇总；隔离条件仍须落实，未将隐藏池改为已激活。
- E只按完整可见且主统计字段核对充分的整卷年份计算；训练候选池、未标注及隐藏内容不得当作未考，数据不足时暂缓细粒度数值排序。

本次只修改方案说明Markdown，未读取教材/题目原始OCR、未修改机器规范或脚本、未产生作答或测评记录。统计局部完成与离线汇总的机器兼容边界见《系统文件/数据规范/README.md》，不能把文档修订当作自动化能力上线。

## 三、最重要的历史纠错：Teacher 与 Evaluator 隔离

协议要求固定审计题和密封卷对 Teacher 真正不可见。曾经有一轮在共享项目里生成了包含具体年份、题号和路径的隐藏候选清单，并仅用 `teacher_visible:false` 声称隔离。

审查后确认这是无效设计：

- 字段声明不是访问控制；
- 共享项目中的任何具体身份都已经对 Teacher 侧暴露；
- 当时生成的所有隐藏候选身份必须永久作废，不能换个文件名重新使用；
- 共享项目只能保存不透明池句柄、允许的请求字段和聚合结果接口；
- 具体固定审计、密封卷必须在Teacher不可访问的范围重新选择；可由人工离线保管执行，自动化路径才需要独立存储和进程。对应原文及已知副本也必须满足访问隔离。

详细隐藏候选文件已经从当前共享项目撤下。公开接口保存在 `系统文件/配置/evaluator_public_contract_v1.json`。

没有真正隔离时，`FIXED_AUDIT` 和 `SEALED` 必须保持 blocked/default-deny。公开 Day 0 和 STARTUP 仍可进行，但不能把普通共享文件假装成密封池。

## 四、几个容易混淆但必须分开的概念

### 协议完成不等于系统已经运行

协议和 schema 可以完整，学生模型仍可能是空的。只有出现真实作答、用户自核结果、延迟复测和周审计，系统才开始形成自适应闭环。

### 题库有编号不等于完成 L0—L3

全库8024个唯一编号已经存在，但正式独立 annotation 数据表尚未全量生成。原 Markdown 中的旧“考点”字段只能作为候选证据。

### 整题记录不等于题目原子

数学一和408很多大题仍以整题 ID 存在。协议要求最终原子是可独立判分的小问。没有可靠分值证据时宁可标记待审核，也不能自动拆题或编造分数。

### 真题的统计用途与训练用途不同

- 数学一、408真题可作为目标考试统计候选；
- 数学二、数学三只能作为辅助迁移，不进入数学一目标概率；
- 张宇、王道和408经典题可用于训练和抽题，但不能证明目标考试先验；
- STARTUP卷使用后永久退出个人能力测量；
- 隐藏测评题退役前不能进入 Teacher 可见统计或训练。

### 锚点数量是候选范围，不是硬 KPI

每门课程24—36是名义候选规划范围，七门合计168—252。五日/25小时的时间盒优先；实际题目原子不足或时间不足时，硬最低为0。

## 五、目录为什么这样整理

用户明确要求文件夹名称直接表达内容，不能靠猜。2026-08-31进行了一次本地整理：

- `核心真题/` 只保留数学一和408目标真题；
- `辅助真题/` 放数学二、数学三；
- `配套习题/` 放可用于训练和抽题的非目标题库；
- `讲义/` 就是教材；
- 所有配置、规范、运行记录、检查报告、迁移、标签、脚本和归档统一收进 `系统文件/`；
- 旧的 `delightful-salk` 被拆开，唯一内容归入正式目录，重复内容保存在 `系统文件/历史归档/`；
- 项目级说明 Markdown 继续直接放在根目录。

历史归档不是当前数据源。新 AI 不应从归档副本抽题、计数或执行脚本。

## 六、当前真实状态

已经完成：

- 主资料目录和题源口径整理；
- 8024唯一题目记录基线；
- 正式 JSON Schema；
- 旧50题非破坏迁移候选；
- 执行合同；
- 公开 Evaluator 接口；
- 空作答日志及验证器；
- Day 0公开准备检查；
- 当前根目录交接文件整理。

尚未完成：

- 全库正式 L0/L1；
- 近期实际调用题的正式L2教学字段，以及合格整卷的统计主字段；
- 用于统计或近期调用的大题必要原子化；
- canonical 知识词典；
- 能力图、考试模型、学生模型和复习队列；
- 真实 STARTUP 作答；
- 满足真实隔离条件的人工离线Evaluator或独立系统，以及私下选定的隐藏池。

截至既有2026-09-03台账，尚无真实attempt，能力状态未建立。本次未读取运行日志或核验实际作答，不将这条历史记录推断为用户当前学习事实。不要根据文件数量推断系统已开始教学。

## 七、新 AI 接手时应该怎么做

1. 先读根目录五份入口文件，不先打开题库正文。
2. 检查 `系统进度.md` 的更新时间和工作树状态，保留用户现有未提交改动。
3. 从 `系统文件/配置/` 和 `系统文件/数据规范/` 读取机器口径，不从历史聊天猜字段。
4. 需要统计时只使用 `系统文件/配置/canonical_sources_v1.json` 指定的 canonical 路径。
5. 需要抽题时先确认用途角色；归档副本、旧50题和已暴露题不能误入隐藏测评。
6. 不读取、生成或验证答案库；等待用户自行核对后回报运行元数据。
7. 如果任务只是整理或设计，不擅自打开真题正文。
8. 每次有实质进展，同轮更新 `系统进度.md`；如果形成新的长期背景，再精炼进本文件。
9. 不能建立真实隔离时，明确报告隐藏测评仍 blocked，不要伪造已完成。
10. 不用 `git reset`、`checkout` 或批量覆盖清理当前工作树。

## 八、如何继续维护这份文件

只记录以下内容：

- 用户新冻结的长期输入；
- 会影响后续判断的设计消歧；
- 重要失败及其根因；
- 已经撤销、污染或永久禁用的产物；
- 目录或数据口径的重大变更；
- 新 AI 仅靠协议、合同和进度仍无法知道的背景。

不要记录：

- 礼貌寒暄；
- 工具调用过程；
- 每一条临时计划；
- 已经在其他权威文件中完整表达、且没有额外背景价值的重复内容；
- 整个聊天窗口逐字稿。

新增内容时优先修改现有段落，只有出现新的长期主题才增加章节。目标是让接手者读完后能正确行动，而不是证明对话发生过。
