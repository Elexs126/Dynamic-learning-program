# 408 2013—2026 年 L2—L3 执行说明

本批完成本地 14 年共 658 题，每年 40 道选择题及 7 道综合题。逐题核对题干与现存解析，并查看全部 162 张引用图片。未联网或调用外部接口。

## 结果及使用

结果目录为 `系统文件/题目标注/408_2013_2026_L3/`，主文件为 `question_annotations_cs408_2013_2026_l23_v1.json`。这是累积 L0—L3 视图，原 L0/L1 逐字段保持。与旧批次 2009—2012 年的 188 题按题号合并后，覆盖核心 408 全部 846 题；不得与 L1 再计一遍。

同目录提供字段来源、评分证据、任务引用、词条、同构关系、覆盖报告、校验报告和哈希清单。逐题核对输入保存在 `cs408_2013_2026_review_v1/`，不含原题、答案或完整解法代码。复跑依赖本项目既有 L1、`build_l1.py`、`json_schema_runtime.py` 及 v1.1 两份规范，仅需 Python 标准库。

## 运行

在项目根目录执行：

```bash
python3 系统文件/脚本工具/05_标注与规范校验/build_cs408_2013_2026_l23.py check
python3 系统文件/脚本工具/05_标注与规范校验/test_cs408_2013_2026_l23.py -v
```

生成另一份独立快照时：

```bash
python3 系统文件/脚本工具/05_标注与规范校验/build_cs408_2013_2026_l23.py build --output /tmp/cs408-2013-2026-l23-review
```

已有输出是不可直接覆盖的快照。程序、来源、图片、规范或核对输入变化后，需要重新核对并生成新版本；不允许假装复用。`check` 会重新生成预期数据并比较全部输入输出哈希。

## 评分与未完成范围

共 729 个核心评分单元。17 题采用 88 个完整明确且一致的分项；另存 195 项本地分值证据及 102 条条件规则。证据不能与核心单元再次相加。其余综合题没有完整可用的拆分，保留整题口径，已知部分照常登记。

15 题因分值冲突将有效总分留空，字段 `l3.score_units.S0.points` 设为局部待核对。2013 第41题的部分条件评分适用范围也待确认。其余可判断标签正常完成。2026 第41、44题虽然元数据与分项合计相同，但题干另标不同满分，仍按冲突处理。2024 第44题前两问分值已等于元数据，而第三问未标分，不将第三问推算为零分。

本批 L2/L3 没有因整类信息缺失而整体跳过的标签类型。缺少独立出处核验资料，L0 来源可靠度保持 `unknown`，没有给本地文件猜测权威等级。原答案争议和普通质量注记不等于整个标签省略，也不表示本轮建立了答案认证。

## 任务和关系

875 个任务引用用于定位，不是新增原题或独立评分原子。登记 21 个备选方法、75 条直接结果依赖和 20 个同构簇（42题）。共用前题图表与定义记录在 `context_sources`；实际调用前题结果才写为跨题依赖，并检查全局无环。同构只覆盖声明的本批共同结构，不修改旧批次关系。

`verified` 表示标签已按本地内容逐题核对，不表示人工签核、原解答认证、实测难度或正式考频资格。整条记录的 `needs_review` 保留原 L0/L1 候选状态，使用时应看逐字段状态。未设置用途角色或新增作答记录。

## 检测

24 项回归检查通过，覆盖整批范围、分值及正文满分冲突、条件分非累加、无依据拆分拒绝、出处图片及评分锚点、跨题依赖环、共用上下文、语义输入一致性、同构互引和不可变快照。旧 408 的188题、数学一431题与全库L1的8024题复跑检查全部通过。检查文件位于 `系统文件/检查报告/cs408_2013_2026_l23_2026-09-09_*`。
