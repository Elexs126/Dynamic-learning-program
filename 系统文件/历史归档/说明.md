# 历史归档说明

本目录只保存为追溯而保留的旧内容，不是当前资料入口。

- `原始对话记录_历史备份.md`：整理前的聊天逐字堆积。
- `早期方案/`：已经被正式协议、规范或当前进度替代的讨论稿。
- `重复副本/`：与正式目录逐文件相同的旧副本；不得参与题库计数、抽题或脚本调用。
- `旧输出目录/`：为兼容历史路径保留的链接。
- `空占位文件/`：过去存在但没有实际内容的文件。
- `旧目录结构/`：清理旧英文目录后保留的空结构。

当前状态请读根目录 `系统进度.md`；当前数据路径请读 `系统文件/配置/canonical_sources_v1.json`。
