我逐条检查了这份草案。整体方向已经正确，但还不能原样定稿。除了它最后提出的两个问题，我还发现了几个必须修正的地方。

最重要的最终决定先说：

1. `audited_main_ability` 删除，不作为独立字段。
2. `candidate_main_ability` 改成 `candidate_main_knowledge`，避免和能力行为混淆。
3. `main_knowledge` 表示最终规范知识点。
4. `assessed_construct` 表示学生必须表现出的能力行为。
5. `source_reliability` 归 L0，只评价原始题目来源。
6. `data_role` 不属于永久知识标签，单独保存为题目使用角色历史。
7. 当前核心 L2 全量范围确定为：数学一真题 431 + 张宇基础 30 讲 876 + 408 真题 846，共 2153 题。
8. 但目标考试统计仍然只使用数学一和 408，共 1277 题；张宇 30 讲是核心训练来源，不进入目标考试考频和期望分值统计。
9. L3 不对所有题一次性完成，只对实际进入精标池的题完成。

下面是修正后的正式定义。

# 题库标注深度 L0—L3 正式执行定义

四层标注逐层累积：

```text
L0 原始事实
  ↓
L1 候选粗标
  ↓
L2 审核后的正式知识与教学标签
  ↓
L3 评分、证据和题目关系
```

完成 L3 的题目必须同时保留 L0、L1、L2、L3，不能只保存 L3。

## L0：原始事实层

L0 只回答：

> 这到底是哪一道题，它从哪里来，原始文件是什么状态？

L0 尽量由脚本直接提取，不依赖 AI 推理。

### `question_id`

全局唯一题目 ID。

例如：

```text
M1-08-C-T08
M1-08-F-T09
M1-08-A-T15
408-09-C-T01
WD-DS-C-2.2-T03
ZY30-GS-01-L1.01
```

数学一、数学二、数学三的 ID 必须包含题型代码 `C/F/A`，不能写成含义不完整的 `M1-08-T08`。

### `exam_track`

题目属于哪条考试轨道：

```text
MATH1
MATH2
MATH3
CS408
```

### `subject_area`

题目属于哪一个学科分区：

```text
calculus
linear_algebra
probability
data_structure
computer_organization
operating_system
computer_network
```

`exam_track` 和 `subject_area` 不是一回事。

例如：

```text
exam_track: MATH1
subject_area: calculus
```

### `question_source`

题目本身来自哪里。

例如：

```text
数学一 2009 年官方真题
408 2021 年官方真题
张宇基础30讲
张宇1000题
王道408
408经典练习题
```

这个字段只说明题目来源，不说明标签怎么判断。

### `source_reliability`

评价原始题目来源本身的可靠程度。

例如：

```text
high
medium
low
unknown
```

含义：

```text
high
= 官方试卷、权威出版物或可以直接核对的原始资料

medium
= 较可靠的教材、整理资料或有明确出处的网页

low
= 来源不明、二次转载、OCR 严重损坏或无法核对

unknown
= 暂时没有足够信息判断
```

它不表示 AI 标签是否正确。标签正确程度用 `confidence` 表示。

### `year`

真题年份。

教辅题没有真实考试年份时填写：

```text
not_applicable
```

不能凭空制造年份。

### `question_number`

原始题号，例如：

```text
T08
第41题
例1.1
习题03
```

### `question_type`

统一后的题型，只使用：

```text
choice
fill
analytical
```

“证明题”“计算题”“综合题”不是同一级题型，而是任务形式或结构描述。必要时另设：

```text
task_form
```

不能把“泰勒展开”“矩阵秩”写进 `question_type`。

### `original_section`

题目在原始题源中的章节或专题。

例如：

```text
张宇30讲 > 高等数学 > 第1讲
王道408 > 数据结构 > 2.2 顺序表
```

它表示原书怎么组织题目。

### `question_completeness`

题干文件完整性：

```text
complete
incomplete
needs_review
```

检查：

- 文字是否完整；
- 公式是否损坏；
- 图片是否缺失；
- 选项是否完整；
- 子问题是否缺失。

### 解析完整性

解析是否完整可以作为独立的文件摄取质量检查，但不属于 L0—L3 的知识标签。

最终 L3 文件不能保存：

```text
answer
solution
reference_answer
reference_solution
完整解析
```

原始答案和解题步骤可以被读取作为判断证据，但不能写入最终标签结果。

---

## L1：候选粗标层

L1 覆盖全部题目。

L1 只回答：

> 快速看这道题时，它大概属于哪里、大概考什么、是否可能和其他记录重复？

L1 的结果都是候选结果，不是最终结论。

### `official_scope_coarse`

按统一考纲或课程骨架给出的粗粒度路径。

例如：

```text
高等数学 > 函数、极限与连续
408 > 数据结构 > 线性表
```

它不是：

- 原始题源章节；
- 最细知识点；
- 解题方法；
- 难度。

`original_section` 回答“原书放在哪里”。

`official_scope_coarse` 回答“按统一考纲应该放在哪里”。

如果后续需要更细的路径，可以在同一个 `official_scope` 对象中增加：

```text
official_scope.precise
```

但 `precise` 不是 L1 的强制要求，不能因为少填了它就判定 L1 不完整。

### `label_provenance`

说明候选标签依据什么产生。

例如：

```text
official_syllabus
primary_textbook
question_semantics
unofficial_index
ai_inference
human_review
```

它回答：

> 我们为什么把这道题暂时归到这里？

它和 `question_source` 完全不同：

```text
question_source
= 题目本身从哪里来

label_provenance
= 标签判断依据什么
```

### `candidate_main_knowledge`

这是原来“候选主能力”的机器字段。

之所以改名，是因为它最终对应的是 `main_knowledge`，而不是 `assessed_construct`。

它回答：

> 如果现在只能先猜一个，这道题大概主要属于哪个知识点？

例如：

```text
高阶极限中的首个非零项判断
```

它只是候选判断，不能直接用于最终考频统计。

关系是：

```text
L1 candidate_main_knowledge
        ↓ 审核
L2 main_knowledge
```

### `confidence_by_field`

置信度必须针对具体字段。

例如：

```json
{
  "official_scope_coarse": "high",
  "candidate_main_knowledge": "medium",
  "duplicate_candidate": "low"
}
```

标准：

```text
high
= 基本不存在合理竞争解释

medium
= 基本判断明确，但存在一种合理替代解释

low
= OCR、图示、题干或来源存在明显不确定
```

它不表示题目难度，也不表示来源可靠度。

### `duplicate_candidate`

表示这道题是否可能和另一条记录是同一道题或轻微改写版本。

例如：

```json
{
  "is_candidate": true,
  "possible_duplicate_ids": ["ZY30-GS-01-X03"],
  "basis": "题干、数据和问法高度一致",
  "status": "candidate"
}
```

重复题和同构题必须区分：

```text
duplicate
= 两条记录可能其实是同一道题

isomorphic
= 两道不同题，但核心解题结构相同
```

同一章、同一知识点、同一种方法，不足以判定重复。

---

## L2：审核后的正式知识与教学标签

L2 的范围是：

- 核心题库全量；
- 其他来源按照优先级分层处理。

按当前项目优先级，核心 L2 全量为：

```text
数学一真题：431
张宇基础30讲：876
408真题：846
合计：2153
```

其中：

```text
数学一真题 + 408真题 = 1277
```

用于目标考试考频、分值和期望分统计。

张宇基础 30 讲虽然属于核心训练来源，但不能并入数学一目标考试统计。

数学二、数学三、张宇1000题、王道408、408经典练习题等，不做第一批全库精细 L2；它们按层级或实际需要调用。

### `main_knowledge`

这是审核后的唯一主知识点。

它回答：

> 如果只能保留一个得分目标，这道题主要是为什么知识点给分？

例如：

```json
{
  "id": "M1_LIMIT_HIGH_ORDER_LOCAL_BEHAVIOR",
  "name": "高阶极限中的首个非零项判断"
}
```

每个题目原子必须有且只有一个主知识点。

主知识点可以用于：

- 目标考频统计；
- 主分值统计；
- Teacher 能力包；
- 学习前沿；
- 主错误诊断。

### `assessed_construct`

这是学生必须表现出的具体能力行为。

例如：

```text
判断需要展开到哪一阶，并提取首个非零项。
```

或者：

```text
识别主导阶，并判断参数使两个无穷小等价。
```

二者的区别必须固定：

```text
main_knowledge
= 题目属于哪个知识点

assessed_construct
= 学生要做出什么行为，才能证明自己会这个知识点
```

`assessed_construct` 不是第二个知识点，也不参与独立考频统计。

### `secondary_knowledge`

除主知识点之外，只有在以下情况下才记录次知识点：

- 它参与关键推理；
- 它对应明确评分步骤；
- 题目明确要求跨知识点迁移。

数量限制：

```text
选择题、填空题：最多 1 个次知识点

解答题：最多 2 个次知识点
```

不能因为题目过程中顺手用了某个公式，就把它列为次知识点。

次知识点不等于：

```text
前置能力
主要方法
共学任务簇
同构关系
```

### `primary_method`

主要解法骨架。

例如：

```text
局部展开并比较首个非零项
建立特征方程并按根的情况构造解
利用高斯公式将通量转化为体积分
```

它回答“主要怎么做”，不回答“主要考什么”。

L2 只记录方法骨架，不保存完整答案或完整解题过程。

### `prerequisites`

题目主任务开始前需要的关键能力。

分为：

```text
hard_prerequisite
= 缺少它就基本无法完成主任务

soft_prerequisite
= 有它会更容易，但没有它不一定无法完成
```

不能把所有用过的基础公式都列成前置能力。

### `difficulty_band`

题目自身的静态难度，不是学生当天的个人感受。

暂定四档：

```text
basic
intermediate
advanced
very_advanced
```

判断标准：

```text
basic
= 单一知识点直接调用，步骤短，几乎无隐藏辨型

intermediate
= 一个主知识点，调用标准前置，需要连续多步

advanced
= 存在隐藏辨型、明显迁移、边界处理或多个关键步骤

very_advanced
= 多知识点深度综合，方法不明显，证明或建模负荷很高
```

不能因为题目分值高、学生第一次做错，就自动判为高难。

---

## 使用角色不属于永久知识标签

`data_role` 或 `practice_role` 表示：

> 系统当前拿这道题做什么？

它不是题目永久的知识属性。

使用角色包括：

```text
RAW
TARGET_PRIOR
STARTUP_TRAINING_ONLY
TRAIN
ADAPTIVE_DIAGNOSTIC
FIXED_AUDIT
SEALED
AUX_OFFPOLICY
```

正式存储应当与 `question_tags` 分离：

```text
question_tags
= main_knowledge
  assessed_construct
  secondary_knowledge
  primary_method
  prerequisites
  difficulty_band

question_usage_roles
= 题目在不同时间、不同视图中的使用角色
```

如果角色发生变化，不能覆盖旧记录，而应保留历史：

```json
[
  {"role": "RAW"},
  {"role": "TARGET_PRIOR"},
  {"role": "TRAIN"}
]
```

---

## L3：评分、证据和题目关系

L3 只对实际进入精标池的题目执行，包括：

- STARTUP 锚点；
- 当前训练题；
- 诊断题；
- 固定审计题；
- 实际即将调用的题；
- 需要评分或同构分析的题。

这里的“真题”主要指目标考试真题：

```text
数学一
408
```

数学二和数学三属于辅助来源，不做全库 L3；只有实际调用时才做。

数学一和 408 也不是一开始全部做 L3。它们先完成 L0/L1，L2按核心范围处理，L3按实际调用处理。

### `score_units`

能够独立判分的最小题目单位。

规则：

```text
选择题通常是一个评分单元
填空题通常是一个评分单元
解答题只有明确独立小问或可靠独立评分依据时才拆
```

不能为了追求精细，凭感觉把 4 分题拆成：

```text
1分 + 1分 + 2分
```

如果没有可靠的子分值证据，就保持整体单元，并标记分值细分未确定。

### `score_attribution`

说明评分单元的分值归属于什么：

```text
主知识点
次知识点
方法操作
综合目标
无法确定
```

主知识点考频统计和评分归属不是同一件事。

一道 4 分题仍然只有一个主知识点；内部可以记录多个操作型评分证据，但不能因此创造多个主知识点。

### `evidence_steps`

记录阅卷时可以观察到的关键得分证据。

例如：

```text
正确建立特征方程
正确识别主导阶
正确处理积分方向
正确写出参数条件
```

它不是：

- 参考答案；
- 完整解题过程；
- 教学解析；
- 给学生的提示。

### `alternative_methods`

记录除了主要方法之外，其他确实可行的解法。

例如：

```text
primary_method:
泰勒展开

alternative_methods:
洛必达法则
等价无穷小比较
```

只记录方法名称和必要适用条件，不写第二套完整答案。

### `inter_question_dependency`

只有在具体题目 A 的结果被具体题目 B 直接调用时，才标注题间依赖。

例如：

```text
同一大题第 (2) 问必须使用第 (1) 问的结果
```

以下都不算题间依赖：

- A 和 B 同一章；
- A 和 B 同一知识点；
- A 比 B 简单；
- 建议先做 A 再做 B；
- A 的能力是 B 的前置。

没有直接依赖就写：

```text
none
```

### `isomorphic_relation`

记录不同题目之间的同构解题结构。

至少说明：

```text
cluster_id
structural_invariant
transfer_axis
```

例如：

```text
cluster_id:
LIMIT_HIGHER_ORDER_01

structural_invariant:
识别主导阶并提取首个非零项

transfer_axis:
函数形式变化、参数变化、题型变化
```

同构不是重复：

```text
重复
= 题目可能是同一道题

同构
= 题目不同，但解题骨架相同
```

---

## 跨层控制字段

### `review_status`

正式状态只有：

```text
candidate
verified
needs_review
not_applicable
```

含义：

```text
candidate
= 初步提出，尚未充分审核

verified
= 已依据题干、考纲、教材或评分证据核对

needs_review
= OCR、图示、来源冲突或存在多个合理解释

not_applicable
= 该字段不适用于这道题
```

`DEMO_ONLY` 只能表示示范记录，不是正式审核状态。

审核状态最好按字段记录：

```text
official_scope_status
main_knowledge_status
score_mapping_status
```

### `confidence`

置信度也应按字段记录，不要只给整题一个总分。

```text
scope_confidence
main_knowledge_confidence
score_mapping_confidence
isomorphic_confidence
```

`confidence` 和 `source_reliability` 永远不能互换。

---

# 最终字段表

## L0

```text
question_id
exam_track
subject_area
question_source
source_reliability
year
question_number
question_type
original_section
question_completeness
```

解析完整性可以作为独立的文件质量字段，但不属于正式知识标签。

## L1

```text
official_scope_coarse
label_provenance
candidate_main_knowledge
confidence_by_field
duplicate_candidate
```

## L2

```text
main_knowledge
assessed_construct
secondary_knowledge
primary_method
prerequisites
difficulty_band
```

## 独立使用角色表

```text
question_usage_roles
```

记录：

```text
RAW
TARGET_PRIOR
STARTUP_TRAINING_ONLY
TRAIN
ADAPTIVE_DIAGNOSTIC
FIXED_AUDIT
SEALED
AUX_OFFPOLICY
```

## L3

```text
score_units
score_attribution
evidence_steps
alternative_methods
inter_question_dependency
isomorphic_relation
```

---

## 对草案最后两个问题的最终回答

### 问题一：L2 核心题全量是否是 431 + 846 + 876？

**按当前项目优先级，是。**

```text
数学一真题：431
张宇基础30讲：876
408真题：846
合计：2153
```

但要严格区分两个口径：

```text
L2 核心训练标注范围：
2153 题

目标考试统计范围：
数学一 + 408 = 1277 题
```

张宇基础 30 讲可以进入核心训练标签，但不能进入数学一目标考频、目标分值和考试覆盖率统计。

### 问题二：`candidate_main_ability` 是否改为 `candidate_main_knowledge`？

**是，应该改。**

最终统一为：

```text
L1 candidate_main_knowledge
        ↓ 审核
L2 main_knowledge
```

同时保留：

```text
L2 assessed_construct
```

用来描述学生需要表现出的能力行为。

不再使用：

```text
audited_main_ability
```

这样以后不会再出现：

```text
候选主能力
审核主能力
主知识点
被测能力
```

四个名称互相重叠的问题。